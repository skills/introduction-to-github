{
  "name": "scrollverse-contracts",
  "version": "1.1.0",
  "description": "ScrollVerse Ecosystem Smart Contracts - Protocol Registry, AnchorManifest, and More",
  "author": "OmniTech1™",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network mainnet",
    "deploy:registry:local": "hardhat run scripts/deploy-protocol-registry.js --network hardhat",
    "deploy:registry:sepolia": "hardhat run scripts/deploy-protocol-registry.js --network sepolia",
    "deploy:registry:mainnet": "hardhat run scripts/deploy-protocol-registry.js --network mainnet",
    "verify:sepolia": "hardhat verify --network sepolia",
    "verify:mainnet": "hardhat verify --network mainnet",
    "clean": "hardhat clean",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  },
  "keywords": [
    "scrollverse",
    "protocol-registry",
    "security-layer",
    "anchor-manifest",
    "merkle-root",
    "ipfs-cid",
    "smart-contracts",
    "ethereum",
    "sepolia",
    "dao-governance"
  ]
}
